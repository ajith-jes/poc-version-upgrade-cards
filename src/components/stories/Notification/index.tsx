
import React from 'react';

import { NotificationProps } from './index.interface';

/**
 * Primary UI component for user interaction
 */
export const Notification = (_props: NotificationProps) => {
  return (
    <div>
      notification
    </div>
  );
};